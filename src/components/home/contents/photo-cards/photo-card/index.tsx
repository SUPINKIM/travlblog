import { type PhotoCard } from "../constant";

const PhotoCard = ({ card }: { card?: PhotoCard }) => {
  return <div className="h-[360px] max-w-screen-sm px-[20px] bg-gray-200" />;
};

export default PhotoCard;
